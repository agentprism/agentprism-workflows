---
"@automatalabs/acp-server": minor
---

Add an HTTP mode that serves ACP V1 over Streamable HTTP and WebSocket, backed by the official TypeScript SDK transport adapters, while retaining stdio as the default.
