---
"@automatalabs/mcp-server": minor
"@automatalabs/workflow-engine": minor
"@automatalabs/workflows": minor
---

Add absolute `scriptPath` delivery, persistence-backed workflow script resources and lineage links, full resource subscription/list-change capabilities, and the `workflow` tool's durable `stop` action. Preserve content-free resume ancestry across run deletion and expose manager deletion observability for resource consumers.
