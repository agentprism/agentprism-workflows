---
"@automatalabs/mcp-server": minor
"@automatalabs/workflows": minor
---

Add a model-facing `docs` tool that returns version-matched workflow and REPL authoring documentation one bounded topic at a time as an embedded MCP resource, with byte-identical static resources and a compact index. Decouple the server from the optional authoring skill and replace the giant `author-workflow` prompt injection with selective topic guidance.
