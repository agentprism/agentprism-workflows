---
"@automatalabs/workflows": minor
---

Add `npx -y @automatalabs/workflows mcp` to launch the embedded AgentPrism stdio MCP server without a separate `@automatalabs/mcp-server` install. The standalone server package remains independently available, and the embedded server reports the workflows package version in `serverInfo.version`.
