---
"@automatalabs/mcp-server": patch
---

Validate the official legacy MCP Apps client capability shape before exposing the run-monitor surface, reject experimental or malformed lookalikes, and document the boundary between legacy initialize negotiation and the separately gated modern `server/discover` path.
