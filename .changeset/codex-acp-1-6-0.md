---
"@automatalabs/acp-agents": patch
---

Bump `@automatalabs/codex-acp` to 1.6.0 (upstream sync: codex 0.144.1, configurable LLM providers — the fork now advertises `providers` and implements `providers/list|set|disable` — and capitalized reasoning-effort labels).
