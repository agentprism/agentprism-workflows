---
"@automatalabs/acp-agents": patch
---

Refresh the wrapped Claude Agent SDK runtime from 0.3.246 to 0.3.247. The upstream release adds ambient background-task metadata and spinner-tip configuration plus internal CLI fixes; the ACP-integrated structured-output, stop-reason, and usage contracts remain unchanged.
