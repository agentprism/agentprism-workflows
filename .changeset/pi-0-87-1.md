---
"@automatalabs/pi-acp": patch
---

Embed pi 0.87.1 (`@earendil-works/pi-ai`, `pi-coding-agent`, and `pi-agent-core` from 0.87.0). The release adds Claude Opus 5.5, GPT-6 Sol, and GPT-6 Luna for supported providers, makes Grok 4.7 the default xAI model, and fixes split-turn compaction summaries, image-only user messages on some OpenAI-compatible providers, and the Claude Code version reported on Anthropic OAuth requests. The authentication guidance and the retry, overflow, and error-body sources that pi-acp's error classification keys on are byte-identical to 0.87.0, so pause/retry classification is unchanged.
