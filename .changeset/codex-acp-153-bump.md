---
"@automatalabs/acp-agents": patch
---

Bump the pinned `@automatalabs/codex-acp` to 1.5.3 (upstream sync: Codex CLI 0.144.0 pairing, ACP SDK 1.2.1, MCP elicitation support, agent message phases). Restores structured output on the default-backend routing path with current Codex CLI installs.
