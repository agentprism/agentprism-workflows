---
"@automatalabs/mcp-server": minor
---

Consolidate workflow run observation under the canonical `status` action with optional request-bounded `waitMs`, while normalizing legacy `inspect` and `await` requests during migration.
