---
"@automatalabs/shared-types": minor
---

Add the isolation/replay seam contract: per-call identity and provenance callbacks, frozen call-manifest and recorded-error types, journal metadata, honest run-result fields, and the `RECORDING_UNUSABLE`, `REPLAY_TARGET_INVALID`, and `REPLAY_DIVERGENCE` error codes.
