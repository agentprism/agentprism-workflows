---
"@automatalabs/mcp-server": major
---

Publish the workflow tool as a strict seven-action discriminated JSON Schema whose runtime validator rejects cross-action fields, while retaining unadvertised migration normalization for omitted-action run and retired inspect/await requests.
